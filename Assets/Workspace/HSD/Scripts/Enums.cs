
namespace Database
{
    public enum UserDataType { Name, Win, Lose, Connected, Gem }
}

namespace Game
{
    public enum MapType { A, B, C, Length }
    public enum Team { Red, Blue, Wait }
    public enum State { Login, Lobby, Game }
}
